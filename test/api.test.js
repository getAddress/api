const assert = require('assert');
const getAddress = require('../dist/getaddress-api-cjs');
const apiKey = require('./api-key');

const api = new getAddress.API(apiKey.getKey());

describe('Find',()=>{

    
});